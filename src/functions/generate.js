export function Generate(input){
    let systemMessageStart = "Please make a rental listing descrition for: "
    let systemMessageEnd = ". Make Sure you leave a blank for the price so that I can fill it in."
    let systemMessage = systemMessageStart + input + systemMessageEnd

    /// post req
    /// get res
    /// parse value

    return systemMessage
}